export PATH=/tmp/cross-tools/bin:/opt/gcc440/bin:$PATH
export LC_ALL=POSIX
unset CFLAGS
unset CXXFLAGS

mkdir -p /tmp/Build/logs

#time ruby -I $HOME/gits/litbuild/lib build-toolchain-uclibc.rb
time ruby -I $HOME/gits/litbuild/lib build-toolchain-glibc.rb
#time ruby -I $HOME/gits/litbuild/lib build-toolchain-eglibc.rb
